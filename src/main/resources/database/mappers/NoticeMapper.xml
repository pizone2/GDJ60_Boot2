<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.iu.base.board.notice.NoticeDAO">
 
 	<sql id="search">
 		<where>
 			<if test="kind == 'title'">
 				TITLE LIKE CONCAT('%',#{search},'%')
 			</if>
 			<if test="kind == 'contents'"> 			
 				CONTENTS LIKE CONCAT('%',#{search},'%')
 			</if>
 			<if test="kind == 'id'">
 				ID LIKE CONCAT('%',#{search},'%')
 			</if>
 		
 		</where>
 	
 	</sql>
 	
  	<select id="getTotalCount" parameterType="Pager" resultType="Long">
 		SELECT COUNT(NUM)FROM NOTICE
 		<include refid="search"></include>
 	</select> 
 
 	<select id="getList" parameterType="Pager" resultType="BoardVO">
 		SELECT * FROM NOTICE 
 		<include refid="search"></include>
 		ORDER BY NUM DESC
 		LIMIT #{startRow},#{perPage}
 	</select>
 	
 	 <select id="getDetail" parameterType="BoardVO" resultMap="getDetailResult">
 		SELECT *
 		FROM NOTICE N
 			LEFT JOIN
 			NOTICEFILES NF
 			USING(NUM)
 		WHERE NUM=#{num}
 	</select>
 	
 	<resultMap type="NoticeVO" id="getDetailResult">
 		<id property="num" column="NUM"/>
 		<result property="title" column="title"/>
 		<result property="id" column="id"/>
 		<result property="contents" column="contents"/>
 		<result property="datecreated" column="datecreated"/>
 		<result property="hit" column="hit"/>
 		<collection property="boardFileVOs" javaType="List" ofType="BoardFileVO">
 			<id property="fileNum" column="fileNum"/>
 			<result property="fileName" column="fileName"/>
 			<result property="oriName" column="oriName"/>	
 		</collection>
 	</resultMap> 
 	
 	<select id="getFileDown" parameterType="BoardFileVO" resultType="BoardFileVO">
 		SELECT * FROM NOTICEFILE WHERE FILENUM=#{fileNum}
 	</select>
 	
 
 	<insert id="setInsert" parameterType="BoardVO" useGeneratedKeys="true" keyProperty="num">
 		INSERT INTO NOTICE (NUM,ID,TITLE,CONTENTS,DATECREATED,HIT)
 		VALUES(#{num},#{id}, #{title}, #{contents}, now(), 0)
 	</insert>
 	
 	<insert id="setBirth">
	 	INSERT INTO NOTICE (ID, TITLE, CONTENTS, DATECREATED, HIT)
	 	VALUES ('관리자', '생일 축하합니다! ', 
		  (SELECT CONCAT(GROUP_CONCAT(USERNAME SEPARATOR ', '), ', 츄카포카') FROM MEMBER 
   		   WHERE DAY(BIRTH) = DAY(CURDATE()) AND MONTH(BIRTH) = MONTH(CURDATE())),
  		CURDATE(), 0);
	</insert>
 	
 	<update id="setUpdate" parameterType="BoardVO">
 		UPDATE NOTICE SET TITLE=#{title},CONTENTS=#{contents}
 		WHERE NUM=#{num}
 	</update>
 
 	<delete id="setDelete" parameterType="BoardVO">
 		DELETE NOTICE WHERE NUM=#{num}
 	</delete>
 	
 	<!--NoticeFile 에 Insert  -->
 	<!--  <insert id="setFileInsert" parameterType="BoardFileVO">
 		INSERT INTO NOTICEFILES
 		VALUES(#{fileNum},#{num},#{fileName},#{oriName},)
 	</insert>  -->
 	
	   <insert id="setFileInsert" parameterType="BoardFileVO" >
	   INSERT INTO NOTICEFILES (NUM , FILENAME, ORINAME)
	   VALUES(#{num},#{fileName},#{oriName})
   </insert>
 
 	
 </mapper>